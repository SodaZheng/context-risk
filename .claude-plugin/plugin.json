{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "context-risk",
  "displayName": "ContextRisk",
  "description": "Watches context usage and automatically creates structured handoffs at risk thresholds so work can continue in a clean window.",
  "version": "0.1.0",
  "author": {
    "name": "soda"
  },
  "skills": "./skills",
  "defaultEnabled": true,
  "userConfig": {
    "softBlockThreshold": {
      "type": "number",
      "title": "Soft block threshold",
      "description": "Legacy compatibility threshold retained from earlier guardrail behavior.",
      "default": 50,
      "min": 1,
      "max": 99
    }
  }
}
